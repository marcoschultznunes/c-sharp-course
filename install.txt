The C# course will not use the MonoDevelop IDE, as it lacks updates.

The course will use VSCode with some extensions:
=> C# extension by Microsoft
=> Intellicode by MS
=> NuGet Package Manager

You need to install .NET CORE

.NET CORE:
https://docs.microsoft.com/pt-br/dotnet/core/install/linux-ubuntu
